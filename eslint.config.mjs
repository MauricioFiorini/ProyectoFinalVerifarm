import { defineConfig, globalIgnores } from "eslint/config";
import nextVitals from "eslint-config-next/core-web-vitals";
import nextTs from "eslint-config-next/typescript";

const eslintConfig = defineConfig([
  ...nextVitals,
  ...nextTs,
  {
    rules: {
      // Convencion del proyecto: sin `any`. Si aparece un `any`, falta modelar
      // algo. Ver docs/CONVENCIONES.md, seccion 9.
      "@typescript-eslint/no-explicit-any": "error",
      // Variables sin usar: error, no advertencia. Se permite el prefijo `_`
      // para los parametros que hay que declarar pero no se usan.
      "@typescript-eslint/no-unused-vars": [
        "error",
        {
          argsIgnorePattern: "^_",
          varsIgnorePattern: "^_",
          caughtErrorsIgnorePattern: "^_",
        },
      ],
    },
  },
  // Override default ignores of eslint-config-next.
  globalIgnores([
    // Default ignores of eslint-config-next:
    ".next/**",
    "out/**",
    "build/**",
    "next-env.d.ts",
  ]),
]);

export default eslintConfig;
